import { } from '../../services/gradingFormat.service';

describe('addGradingFormat', () => {
    test('', () => {

    });
});

describe('getGradingFormatByType', () => {
    test('', () => {

    });
});