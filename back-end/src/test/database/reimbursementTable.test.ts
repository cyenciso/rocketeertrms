import { deleteTable, createTable } from '../../database/reimbursementTable';

describe('deleteTable', () => {
    test('', () => {

    });
});

describe('createTable', () => {
    test('', () => {

    });
});