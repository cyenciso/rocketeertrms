import { deleteTable, createTable } from '../../database/gradingFormatTable';

describe('deleteTable', () => {
    test('', () => {

    });
});

describe('createTable', () => {
    test('', () => {

    });
});