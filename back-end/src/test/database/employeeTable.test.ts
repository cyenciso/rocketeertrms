import { deleteTable, createTable } from '../../database/employeeTable';

describe('deleteTable', () => {
    test('', () => {

    });
});

describe('createTable', () => {
    test('', () => {

    });
});